<div class="card">
  <h1 class="card-title">Studios</h1>

  <% @studios.each do |studio| %>
    <div class="card-game">
      <div class="card-game-infos">
        <div>
          <h2><%= studio.name %></h2>
          <p><%= studio.description%></p>
          <p><%= studio.postal_address %></p>
          <p><%= studio.origin_country %></p>
        </div>
        <div class="card-link-right">
          <%= link_to "Voir le studio", studio_path(studio), class: "btn btn-gradient", rel: "noopener" %>
        </div>
      </div>
    </div>
  <% end %>

  <div class="card-buttons">
    <%= link_to "Voir les consoles", platforms_path, class: "btn btn-access" %>
    <%= link_to "Ajouter un studio", new_studio_path, class: "btn btn-add" %>
    <%= link_to "Voir les jeux", games_path, class: "btn btn-access" %>
  </div>
</div> <!-- fin .card -->
